*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Scraped prose — fetched locally, never redistributed
/data/local/
/scripts/.cache/

# Claude Code session scratch (lock files, scheduled task state)
/.claude/

# Playwright
/test-results/
/playwright-report/
/blob-report/

# Infection run artifacts
/.artifacts/

# Full scraped catalog: ~943 spells across ~20 books, most NOT Creative Commons.
# Built locally with `npm run scrape`. Only the CC-BY SRD 5.2.1 subset in
# data/srd/ is redistributed.
/data/index/
/.worktrees/
